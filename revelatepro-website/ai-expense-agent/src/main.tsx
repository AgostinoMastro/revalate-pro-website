import React from 'react'
import ReactDOM from 'react-dom/client'
import BetaExpenseAgentLanding from './BetaExpenseAgentLanding'
import '../../src/index.css'

ReactDOM.createRoot(document.getElementById('beta-landing-root')!).render(
  <React.StrictMode>
    <BetaExpenseAgentLanding />
  </React.StrictMode>,
)
